name: Create release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Install zip
        run: sudo apt install zip

      - name: Get latest code
        uses: actions/checkout@v4

      - name: Bundle files
        run: |
          mkdir -p ./temp/pulsar-restaurant
          cp ./{README.md,fxmanifest.lua} ./temp/pulsar-restaurant
          cp -r ./{client,server,configs} ./temp/pulsar-restaurant
          cd ./temp && zip -r ../pulsar-restaurant.zip ./pulsar-restaurant

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: "PULSAR-RESTAURANT ${{ github.ref_name }}"
          body: |
            ![Pulsar Framework](https://r2.fivemanage.com/GPYOH8Hq4GPyAY7czrgLe/pulsarbanner.png)

            ## PULSAR-RESTAURANT ${{ github.ref_name }}

            Player-run restaurant and food service job for Pulsar Framework.

            ### Installation
            1. Download `pulsar-restaurant.zip` below
            2. Extract into your server's `resources` folder
            3. Add `ensure pulsar-restaurant` to your `server.cfg`

            ---
            ![Pulsar Framework](https://img.shields.io/badge/Pulsar-Framework-7c3aed?style=flat-square)
            ![Built for FiveM](https://img.shields.io/badge/Built_for-FiveM-F40552?style=flat-square)
          files: pulsar-restaurant.zip
          prerelease: false
